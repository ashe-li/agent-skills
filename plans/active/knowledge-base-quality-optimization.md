# Implementation Plan: Knowledge Base Quality Optimization

> Generated by /design on 2026-03-14
> Plan file: plans/active/knowledge-base-quality-optimization.md
> Status: PARTIALLY COMPLETE — Phase 1 全數完成、Phase 2 已無可執行步驟（S2.2 於 2026-09-09 作廢）、Phase 3 剩 1 步（S3.1）
> 2026-07-04 修訂：去 ECC 化重寫（原版依賴 ECC learn-eval，與解耦決策衝突）

> **2026-09-09 註記（v3.0.0 前置）**：`curation` 已於 PR #66（commit c948b32）移除，S2.2 與所有 `/curation` 相關要求隨之作廢，明細見下方「作廢範圍（不可執行，2026-09-09）」區塊——該區塊不會被 `/plan-run` parser 解析成 step，其餘項目效力不變。

## Overview

知識庫品質下降的根因：`/update` 的知識提取邏輯只提取 code patterns，遺漏對話中的決策脈絡；交叉比對步驟只被動報告差異，不主動寫入 MEMORY.md。本計畫透過三個獨立可交付的 Phase 修復：擴展提取範圍、強化主動寫入、建立品質管控。

## 根因對照表

| 根因 | 影響 | 解法 Phase |
|------|------|-----------|
| 知識提取範圍過窄 | 對話中的決策脈絡、trade-offs、放棄方案被遺漏 | Phase 1 |
| `/update` 交叉比對步驟被動報告，不主動寫入 | MEMORY.md 空置，專案級決策無持久化 | Phase 1 |
| learned skills 格式不一致 | 部分無 frontmatter、評分格式不統一，難以檢索 | Phase 2 |
| learned skills 無品質管控 | 過時/過度特定的 patterns 累積 | Phase 2 |

**已排除的方案（architect 審查後，2026-03-14 決策，architect agent 已於 2026-04-01 移除）：**
- `/pr` Context 區塊自動回流知識庫 -- 與知識提取來源重疊，時序衝突
- 新增獨立的「對話 context 提取 agent」 -- 與知識提取步驟重疊，token 成本高
- Learned skills 自動合併/過時偵測 -- 風險過高，需人工判斷

## 內建資源（原 ECC Resources，2026-07-04 去 ECC 化改寫）

| Resource | Type | Usage |
|----------|------|-------|
| `general-purpose`（fresh-context 驗收 agent） | agent | Phase 3 S3.1：修改後的 SKILL.md 端對端驗收 |
| `update/SKILL.md` Step 3/4/5（inline 提取範圍 + frontmatter 規範 + 5 維度評分） | 內建流程 | Phase 1 的強化對象（已完成，見下） |

## Industry & Standards Reference

| 技術決策 | 參照依據 | 類型 |
|----------|---------|------|
| Decision context 的知識價值 | Anthropic Constitutional AI：記錄「選 A 而非 B」比只記錄「A 是好的」學習效益更高 | 業界實踐 |
| Memory 三層架構（session/project/global） | Knowledge Lifecycle in AI Systems — 短期→中期→長期分層設計 | 學術研究 |
| YAML frontmatter 格式 | GitHub Pages、Obsidian、Hugo 的事實標準 | 業界標準 |
| 定期 curation 優於無限累積 | Google Brain knowledge distillation：定期淘汰低品質知識更有效 | 學術研究 |

## Implementation Steps

### Phase 1: 強化 `/update` 的知識提取與寫入 — 已完成

**目標：** 讓 `/update` 不只提取 code patterns，也捕獲決策脈絡，並主動寫入 MEMORY.md。

- [x] **S1.1** — 擴展知識提取範圍
  - Files: `update/SKILL.md` Step 3
  - Action: 提取範圍新增決策脈絡、放棄方案、業界/學術依據、對話 before/after 比較；新增提取觸發訊號（「考慮過 X 但...」「最終選擇 Y 因為...」「根據 RFC/OWASP/...」等）
  - Dependencies: []
  - Why: 已由 2026-07-04 update 重寫涵蓋 —— 現行 `update/SKILL.md` Step 3「提取項目」表格與「提取觸發訊號」與本步驟需求逐字對應，無殘留待辦

- [x] **S1.2** — 新增知識分類指引
  - Files: `update/SKILL.md` Step 3
  - Action: 依知識類型分流至跨專案 learned skills / 專案 MEMORY.md / 專案文件
  - Dependencies: [S1.1]
  - Why: 已由 2026-07-04 update 重寫涵蓋 —— Step 3「提取項目→目標」映射表 + Step 5「知識庫位置」表已達成分類目的；原案獨立的 Decision Context markdown 模板未採用（改用 AskUserQuestion 現場確認寫入位置，屬解耦精簡後的設計簡化，非遺漏）

- [x] **S1.3** — 交叉比對步驟從被動報告改為主動寫入
  - Files: `update/SKILL.md`（原案 Step 4，現行 Step 5）
  - Action: 差集 >0 時主動起草修正內容，AskUserQuestion 確認後寫入；新增 MEMORY.md 路徑自動定位（project-hash 規則）
  - Dependencies: [S1.1]
  - Why: 已由 2026-07-04 update 重寫涵蓋 —— 現行 Step 5「知識庫交叉比對」的主動起草 + 確認寫入邏輯、以及 project-hash 定位規則，與本步驟需求逐字相符

- [x] **S1.4** — learned skill 寫入格式強制規範
  - Files: `update/SKILL.md` Step 4
  - Action: 強制 frontmatter（name/description/user-invocable/origin）；品質評分統一 5 維度表格，廢棄單行格式
  - Dependencies: [S1.1]
  - Why: 已由 2026-07-04 update 重寫涵蓋 —— 現行 Step 4 的 frontmatter 區塊與 5 維度評分表格與本步驟需求一致

**Phase 1 依賴：** 全數完成，無殘留待辦。

---

### Phase 2: Learned Skills 品質管控

**目標：** 清理現有 learned skills 的格式問題和廢棄項目。

- [x] **S2.1** — 新增 `/curation` skill（已完成，事後作廢）
  - Files: （無 —— 原產出物已於 PR #66 刪除）
  - Action: 作廢，不執行。本步驟已於 2026-03 完成，產出物已於 PR #66（commit c948b32）刪除；step 僅保留以維持 S3.2 的依賴邊，**不得重新建立該 skill**
  - Dependencies: []
  - Why: 歷史紀錄 —— 當時的 `curation/SKILL.md`（118 行）涵蓋原案五步驟，並多出 TaskCreate 逐項追蹤與修正後 grep 驗證兩項強化；明確不做自動合併相似 skills、不自動判斷過時。該 skill 現已移除，見「作廢範圍」區塊

**Phase 2 依賴：** 與 Phase 1 獨立。S2.1 為歷史完成紀錄（產出物已隨 PR #66 移除，不可重跑），S2.2 已於 2026-09-09 作廢；本 Phase 無剩餘可執行動作。

---

### Phase 3: 驗收與文件更新

- [ ] **S3.1** — 端對端驗收
  - Files: `update/SKILL.md`
  - Action: 用真實 session 執行 `/update`，驗證：Step 3 嘗試提取決策脈絡（不只 code patterns）、Step 5 主動起草 MEMORY.md 寫入內容、HITL 確認流程正常、`/update /pr` pipeline 串接不受影響
  - Agent: `general-purpose`（fresh-context 驗收）
  - Dependencies: [S1.4]
  - Why: 原案 Agent 為 `code-reviewer`（已於解耦計畫移除的 ECC agent）；審查對象是 SKILL.md 文件而非程式碼，依解耦替換原則改用 general-purpose fresh 驗收 agent（程式碼審查場景才改走 `/code-review`，此處不適用）

- [x] **S3.2** — 更新 README + CHANGELOG
  - Files: `README.md`, `CHANGELOG.md`
  - Action: 記錄本次優化的變更
  - Dependencies: [S1.4, S2.1]
  - Why: 已涵蓋 —— README.md 已列出 `/curation`（L41/62/232/529）；CHANGELOG.md 已記錄 `/update` 決策脈絡提取／格式強制／主動寫入變更（L238-247）與 `/curation` 新增（Added 區塊）

## 作廢範圍（不可執行，2026-09-09）

以下只是紀錄，**不是 step**：`/plan-run` 的 parser 只把 `- [ ]` / `- [x]` 開頭且帶 step ID 的行當成 step，本區塊的普通條列一律不會被解析。

- 原 S2.2「首次 curation 執行」 — `curation` 已於 PR #66（commit c948b32）移除，無 skill 可跑，本步驟作廢；已自 Phase 2 移出，且無任何 step 依賴它
- S2.1「新增 `/curation` skill」 — 當時已完成，產出物已隨 PR #66 刪除。該 step 仍留在 Phase 2 只為維持 S3.2 的依賴邊，其 Action 已改為「作廢，不執行」；**注意 parser 不讀 checkbox，`- [x]` 對 `plan_runner.py` 而言仍是 pending**，所以不能只靠打勾來防止重跑
- Acceptance Criteria 的「`/curation` 能掃描 learned skills 並列出格式問題」 — 判定維持「當時已達成」，但該能力自 PR #66 起不復存在
- Risks & Mitigations 的「`/curation` 誤刪有效 skill」 — 狀態改為作廢，不再需要 S2.2 驗證

## Architecture Notes

**設計決策摘要（原 architect 審查記錄，2026-03-14）：**

1. **知識分類邊界**：知識提取（跨專案 patterns）vs MEMORY.md（專案特定決策）的邊界需要在 SKILL.md 中明確說明，避免同一知識重複儲存
2. **PR context 回流已排除**：`/pr` 和 `/update` 分析同一對話，回流會造成重複；應讓 `/update` 做完知識提取後，`/pr` 只負責 PR 輸出
3. **交叉比對步驟強化優先於新增步驟**：既有交叉比對框架，從被動改主動比新增步驟更省 token
4. **project-hash 路徑風險**：MEMORY.md 路徑計算需要 Glob 驗證，SKILL.md 中應明確說明計算規則
5. **文件影響**：update/SKILL.md、README.md、CHANGELOG.md 需更新

## Risks & Mitigations

> Phase 1 相關風險已隨 2026-07-04 update 重寫落地並實際驗證過寫法（見 S1.1-S1.4），下列為原案記錄；S2.2 已作廢，僅 S3.1 的風險仍適用。

| 風險 | 可能性 | 影響 | 緩解方式 | 狀態 |
|------|--------|------|---------|------|
| 交叉比對主動寫入導致 MEMORY.md 膨脹 | 中 | 中 | 只在「有未記錄的新決策」時觸發；HITL 確認把關 | 已落地（Step 5） |
| 知識提取範圍擴展後分類不準確 | 低 | 低 | HITL 確認可攔截；分類表格提供明確判斷標準 | 已落地（Step 3） |
| `/update /pr` pipeline 串接行為改變 | 低 | 中 | Step 5 寫入不影響 Step 7 串接；去重規則表不受影響 | 待 S3.1 驗證 |
| `/curation` 誤刪有效 skill | 低 | 高 | 只自動修正格式；廢棄刪除必須 HITL 確認 | 作廢（2026-09-09，S2.2 不再執行） |
| project-hash 計算錯誤 | 低 | 中 | Glob 驗證路徑存在；失敗時用 AskUserQuestion 讓使用者指定 | 已落地（Step 5） |

## Acceptance Criteria

- [x] `/update` 能識別並提取「決策脈絡」類知識（when conversation contains trade-off discussions）—— 已達成（Step 3）
- [x] `/update` 交叉比對步驟偵測到遺漏時，起草修正內容並請使用者確認後寫入 —— 已達成（現 Step 5）
- [x] `/update` 在專案無 MEMORY.md 時提示建立 —— 已達成（Step 5 MEMORY.md 路徑定位）
- [x] 新寫入的 learned skill 必須包含正確 frontmatter（name/description/user-invocable/origin）—— 已達成（Step 4）
- [x] 品質評分統一為 5 維度表格格式 —— 已達成
- [x] `/curation` 能掃描 learned skills 並列出格式問題 —— 當時已達成（見 S2.1）；該能力已隨 PR #66 移除，不再作為現行驗收項
- [ ] `/update /pr` pipeline 串接不受影響 —— 待 S3.1 驗證
- [x] 修改後的 SKILL.md 檔案不超過 800 行 —— 已達成（`update/SKILL.md` 165 行；當時併計的 `curation/SKILL.md` 118 行已隨 PR #66 移除）

## Review Notes

**計畫品質自審（2026-03-14 原案）：**

| 維度 | 通過 | 備註 |
|------|------|------|
| 完整性 | Yes | 四個根因都有對應步驟 |
| 可執行性 | Yes | 每步有明確的檔案路徑和具體動作 |
| 依賴正確性 | Yes | Phase 1 線性、Phase 2 獨立、Phase 3 依賴 1+2 |
| 粒度適當 | Yes | 每步 1-3 個具體動作 |
| 內建資源合理 | Yes | general-purpose 驗收 agent 用於審查；update/SKILL.md 為優化目標（2026-07-04 由 code-reviewer/learn-eval 改寫） |
| 驗收可測 | Yes | 每個標準可客觀驗證 |
| 業界/學術支撐 | Yes | 4 項參照依據 |
| 實作後工具 | Yes | Phase 3 含驗收 agent 審查和文件更新 |

**2026-07-04 去 ECC 化重寫記錄：** Phase 1（S1.1-S1.4）已由同日 `update/SKILL.md` 重寫全數涵蓋；Phase 2 的 `/curation` skill（S2.1）當時已存在；剩餘待辦為 S2.2（首次 curation 執行）與 S3.1（端對端驗收，agent 改為 general-purpose）。

**2026-09-09 更新：** `curation` 已於 PR #66（commit c948b32）移除，S2.2 及所有 `/curation` 相關要求作廢（見「作廢範圍」區塊），剩餘待辦僅 S3.1。
